import useBudgetStore from "../Stores/store";
const Spend = () => {  

  return (
    <div className="w-full min-w-[296px] p-4 bg-blue-200 rounded text-blue-700 sm:p-6">
      <p className="font-sans sm:text-xl">Spend: {}</p>
    </div>
  );
};

export default Spend;
